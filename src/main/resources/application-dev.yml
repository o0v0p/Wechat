spring:
  datasource:
    url: jdbc:mysql://localhost:3306/chatdb?serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: bingo...333
    driver-class-name: com.mysql.cj.jdbc.Driver
  cache:
    disabled: true  # 开发时禁用缓存
    swagger-ui:
      try-it-out-enabled: true

knife4j:
  production: false
  setting:
    enable-debug: true

logging:
  level:
    root: debug