<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.wechat.dao.UserMapper">
    <resultMap id="BaseResultMap" type="org.example.wechat.pojo.entity.BizUser">
        <id column="sys_user_id" property="userId" />
        <result column="user_name" property="userName" />
        <result column="user_nickname" property="nickname" />
        <result column="user_telephone" property="telephone" />
        <result column="user_password" property="password" />
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO biz_user (sys_user_id,user_name, user_nickname, user_telephone, user_password,
                              user_avatar, user_status, is_deleted, created_time,
                              creator_id, updater_id,updated_time)
        VALUES (#{userId},#{userName}, #{nickname}, #{telephone}, #{password},
                #{userAvatar}, #{userStatus}, #{isDeleted}, #{createdTime},
                #{creatorId}, #{updaterId},#{updatedTime})
    </insert>

    <update id="update" parameterType="org.example.wechat.pojo.entity.BizUser">
        UPDATE biz_user
        <set>
            <if test="userName != null and userName != ''">user_name = #{userName},</if>
            <if test="nickname != null">user_nickname = #{nickname},</if>
            <if test="password != null and password != ''">user_password = #{password},</if>
            <if test="telephone != null">user_telephone = #{telephone},</if>
            <if test="userAvatar != null and userAvatar != ''">user_avatar = #{userAvatar},</if>
            <if test="userStatus != null">user_status = #{userStatus},</if>
            <if test="isDeleted != null">is_deleted = #{isDeleted},</if>
            <if test="userEmail != null">user_email = #{userEmail},</if>
            <if test="userSex != null">user_sex = #{userSex},</if>
            <if test="userSignature != null">user_signature = #{userSignature},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            <if test="updatedTime != null">updated_time = #{updatedTime},</if>
            <if test="updaterId != null">updater_id = #{updaterId},</if>
        </set>
        WHERE sys_user_id = #{userId}
    </update>

    <select id="selectBatchIds" resultMap="BaseResultMap">
        SELECT * FROM biz_user WHERE sys_user_id IN
        <foreach collection="list" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </select>

</mapper>